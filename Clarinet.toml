[project]
name = "redirect-portal"
authors = ["Stacks Developer"]
description = "A decentralized portal management and redirection system"
telemetry = false
requirements = []
[contracts.portal-registry]
path = "contracts/portal-registry.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
